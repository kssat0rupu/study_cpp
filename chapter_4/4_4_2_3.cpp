#include "../std_lib_facilities.h"

int main()
{
  for(int i=0;i<26;++i){
    //cout << char('a'+i) << '\t' << int(char('a'+i)) << endl;
    // cout << char('A'+i) << '\t' << int(char('A'+i)) << endl;
    cout << char('1' +i) << '\t' << int(char('1'+i)) << endl;
  }
}
